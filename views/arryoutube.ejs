<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="">
<title><%= title %></title>
<!-- Latest compiled and minified CSS -->

<link rel="stylesheet"
	href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">
<!-- Optional theme -->
<link rel="stylesheet"
	href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.min.css">


		<!-- 1. Add JQuery and Highcharts in the head of your page -->
        <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js"></script>
        <script src="http://code.highcharts.com/highcharts.js"></script>
        <script src="http://code.highcharts.com/highcharts-more.js"></script>
         
        <!-- 2. You can add print and export feature by adding this line -->
        <!-- <script src="http://code.highcharts.com/modules/exporting.js"></script> -->
        
 
<script type="text/javascript">
				$(document).ready(function() {

			   var payloadData = <%- JSON.stringify(payload[0]) %>;
			   var pradData = <%- payload[0] %>;
			   var sachData = <%- payload[1] %>;
			   
			   console.log("@@@" + pradData.videoId + "@@@");
			   
			   console.log("##############");
			   console.log(payloadData["videoId"]);
			   console.log("##############");
			   console.log(payloadData[0]);
			   var tempobj = payloadData[0];
			   console.log(tempobj.videoId);
			   <% payload.forEach(function(realData) { 
			   		console.log("In loop: " + realData);
		        }); %>
			   var nameData = <%- JSON.stringify(name) %>;
			   //console.log(nameData);
			   
               var xAxisData = pradData.timeData;

               var videoData = pradData.ratingData;
               var videoName = pradData.videoName;
               
               

               var video2Data = sachData.ratingData;
               var video2Name = sachData.videoName;
               
               
               
               console.log(JSON.stringify(videoName));
               console.log(JSON.stringify(videoData));
               
               $('#rating').highcharts({
                title: {
                    text: 'Likability Trend - A.R.Rahman Ok Kanmani',
                    x: -20 //center
                },
                subtitle: {
                    text: 'Source: youtube.com',
                    x: -20
                },
                xAxis: {
                    categories: xAxisData
                },
                yAxis: {
                    title: {
                        text: 'Avg Rating'
                    },
                    plotLines: [{
                        value: 0,
                        width: 1,
                        color: '#808080'
                    }]
                },
                tooltip: {
                    valueSuffix: '/5',
                    pointFormat: "Value: {point.y:.3f}"
                },
                legend: {
                    layout: 'vertical',
                    align: 'right',
                    verticalAlign: 'middle',
                    borderWidth: 0
                },
                series: [{
                    name: videoName,
                    data: videoData
                },
                {
                name : video2Name,
                data : video2Data
               }]
            });
        });
      
</script>

</head>


<body>
	<% include navbar %> 
	<h6></h6>
	
	
	<div style="margin-left: 5%">
		<img border="0" src="../O_Kadhal_Kanmani.jpg" width="1350" height="400">
	</div>
		<div id="viewers" style="min-width: 310px; height: 500px; margin: 0 auto"> </div>
		<div id="blank" style="min-width: 310px; height: 50px; margin: 0 auto"> </div>
		<div id="rating" style="min-width: 310px; height: 400px; margin: 0 auto"> </div>
		<div id="pradeepan" style="min-width: 310px; height: 400px; margin: 0 auto"> </div>
	
</body>

<style>
table,th,td {
	border: 1px solid black;
	border-collapse: collapse;
	border-spacing: 5px;
	text-align: left;
	color: white;
	background-color: #4D4D4D;
}
</style>

<script>

$(document).ready(function(){
    $("#mybutton").click(function () {
       console.log("Clicked");
      var youtubeId = $("#mytext").val();
        var videoInfo = "https://gdata.youtube.com/feeds/api/videos/" + youtubeId + "?v=1";
        $.get(videoInfo).done(function (xml) {
            var rating = xml.getElementsByTagName("rating")[0];
            var statistics = xml.getElementsByTagName("statistics")[0];
            var title = $(xml).find('title').text();
            if (rating) {
                console.log("Average rating: " + rating.getAttribute("average"));
                console.log("Number of Votes: " + rating.getAttribute("numRaters"));
                console.log("Number of views: " + statistics.getAttribute("viewCount"));
                console.log("Title: " + title);
                appendData(youtubeId, title, rating, statistics);
            } else {
                alert("Sorry, no rating.");
            }
        });
    });
});

function appendData(youtubeId, title, rating, statistics) {
	console.log("POST'ing for appending: " + youtubeId + "Rating: " + rating.getAttribute("average"));
	var averageValue = rating.getAttribute("average");
	var numRatersValue = rating.getAttribute("numRaters");
	var viewCountValue = statistics.getAttribute("viewCount");
	var youtubeData = {"average" : averageValue, "numRaters" : numRatersValue, "viewCount" : viewCountValue};
	$.post( "http://localhost:3000/append/" + youtubeId, youtubeData);
}


$(document).ready(function(){
    $("#loadByVideoId").click(function () {
       var youtubeurl = $("#mytext").val();
       console.log("Clicked: " + youtubeurl);
       $.ajax({ 
           type: "GET",
                dataType: "text",
               url: "http://localhost:3000/dataload/"+ youtubeurl,
               success: function(response){        
                  console.log("Got response: " + JSON.stringify(response));   
                  var responseObj = jQuery.parseJSON(response);
                  $('#pradeepan').text((response));
                     
               }
           });
    });
});

</script>
 
<div class="container"><% include footer %></div>

</html>